#include <stdio.h>
int main () {
int number, number2, sum;
printf("Enter two integers: ");
sacnf("%d %d", &number1, &number2);
//calculate thensum
sum=number1 + number2;
printf("%d + %d = %d", number1, number2, sum);
return 0;
}

